# 作業ルール（junichi3112btm 秘書業務）

## 毎回必ず行うこと（2026-08-29 本人指示により恒久化）

1. **作業開始時・事実の提示前に、必ず一次記録を照合する**
   - **Gmail**：全期間検索（`newer_than`で窓を切らない）。応募・締切案件は、プログラム名・主催者名で過去の応募/参加/採択/不採択記録を先に確認する
   - **Google Drive**：`PhaseShift/_memory/MEMORY.md`（記憶索引）と最近更新ファイルを確認する。正本はDrive側にある（gitはコピー・記録用）
   - **Googleカレンダー**：該当期間のイベントを照合する（定型ルーティンは除外してよい）
2. **「締切がある」と「応募すべき」を区別する**：履歴・フェーズ・既存戦略（D-Global軸）との適合を添えて提示する。判断材料がないものは「締切情報」としてのみ伝える
3. **決定事項・方針変更は記録する**：このリポジトリに日付付きmdで記録しコミット・プッシュする。重要なものはDrive `_memory/` にも `reference-*.md` として残す

## 過去の確認漏れ（再発防止の根拠）

- 京都ディープテック起業家育成：2025年度に応募→選出→参加済みだったのに、履歴未照合のまま「応募判断待ち」として提示した
- J-StarX：2026年6月にFounder University Japanへ応募→不採択の記録があったのに、J-StarX UKを適合検証なしで最優先タスクとして提示し続けた

## リポジトリの運用

- 正本ドキュメントはGoogle Drive（`PhaseShift/`配下）。gitには記録・検証ログ・スナップショットを置く
- コミットメッセージは日本語・内容要約形式（既存ログ参照）
- ブランチ：`claude/loving-turing-vr8apt` で開発しプッシュする
